spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    pl.kacosmetology.scheduler: DEBUG
    org.springframework.security: DEBUG

whatsapp:
  sender: dev

